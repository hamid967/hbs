import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { Building2, Plus, ShieldAlert, UserRoundCheck, UsersRound } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

const statusLabels = { active: "نشط", on_leave: "في إجازة", inactive: "غير نشط" } as const;
type EmploymentStatus = keyof typeof statusLabels;

export default function EmployeeDirectory() {
  const utils = trpc.useUtils();
  const { data: employees, isLoading, isError, error } = trpc.employees.list.useQuery();
  const { data: departments } = trpc.employees.departments.useQuery();
  const [departmentOpen, setDepartmentOpen] = useState(false);
  const [departmentName, setDepartmentName] = useState("");
  const [departmentCode, setDepartmentCode] = useState("");
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const [employeeNumber, setEmployeeNumber] = useState("");
  const [jobTitle, setJobTitle] = useState("");
  const [departmentId, setDepartmentId] = useState("none");
  const [managerId, setManagerId] = useState("none");
  const [employmentStatus, setEmploymentStatus] = useState<EmploymentStatus>("active");
  const [joinedAt, setJoinedAt] = useState("");
  const createDepartment = trpc.employees.createDepartment.useMutation({ onSuccess: () => { toast.success("تم إنشاء القسم"); setDepartmentName(""); setDepartmentCode(""); setDepartmentOpen(false); utils.employees.departments.invalidate(); }, onError: issue => toast.error("تعذر إنشاء القسم", { description: issue.message }) });
  const saveProfile = trpc.employees.saveProfile.useMutation({ onSuccess: () => { toast.success("تم حفظ ملف الموظف"); setSelectedId(null); utils.employees.list.invalidate(); }, onError: issue => toast.error("تعذر حفظ الملف", { description: issue.message }) });
  const selected = employees?.find(employee => employee.id === selectedId);
  const openProfile = (employee: NonNullable<typeof employees>[number]) => { setSelectedId(employee.id); setEmployeeNumber(employee.profile?.employeeNumber || ""); setJobTitle(employee.profile?.jobTitle || ""); setDepartmentId(employee.profile?.departmentId ? String(employee.profile.departmentId) : "none"); setManagerId(employee.profile?.managerUserId ? String(employee.profile.managerUserId) : "none"); setEmploymentStatus(employee.profile?.employmentStatus || "active"); setJoinedAt(employee.profile?.joinedAt ? new Date(employee.profile.joinedAt).toISOString().slice(0, 10) : ""); };
  const save = () => { if (!selected) return; saveProfile.mutate({ userId: selected.id, ...(employeeNumber.trim() ? { employeeNumber: employeeNumber.trim() } : {}), ...(jobTitle.trim() ? { jobTitle: jobTitle.trim() } : {}), ...(departmentId !== "none" ? { departmentId: Number(departmentId) } : {}), ...(managerId !== "none" ? { managerUserId: Number(managerId) } : {}), employmentStatus, ...(joinedAt ? { joinedAt: new Date(`${joinedAt}T00:00:00`) } : {}) }); };

  return <DashboardLayout><div dir="rtl" className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold text-[#5d8d70]">القوى العاملة</p><h1 className="mt-2 text-3xl font-bold text-[#1d4532]">دليل الموظفين والأقسام</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-[#748178]">ملفات موظفي شركتك فقط، مع القسم والمدير والحالة الوظيفية في مساحة منظمة قابلة للتوسع.</p></div><Button onClick={() => setDepartmentOpen(true)} className="h-10 rounded-xl bg-[#1f5b45] hover:bg-[#174735]"><Plus className="ml-2 size-4" />قسم جديد</Button></div>{isLoading ? <div className="mt-8 grid gap-4 md:grid-cols-2">{[1, 2, 3, 4].map(item => <Skeleton key={item} className="h-40 rounded-3xl" />)}</div> : isError ? <StateCard title="تعذر تحميل دليل الموظفين" text={error.message} error /> : employees?.length ? <div className="mt-8 grid gap-4 md:grid-cols-2">{employees.map(employee => <article key={employee.id} className="rounded-3xl border border-[#dfe9e1] bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><span className="flex size-11 items-center justify-center rounded-2xl bg-[#e7f3e9] text-sm font-bold text-[#337b54]">{employee.name?.charAt(0) || "م"}</span><span className={`rounded-full px-3 py-1 text-xs font-bold ${employee.profile?.employmentStatus === "inactive" ? "bg-[#f4eeee] text-[#a4514c]" : employee.profile?.employmentStatus === "on_leave" ? "bg-[#f7f0df] text-[#997023]" : "bg-[#e7f4ea] text-[#30774e]"}`}>{statusLabels[employee.profile?.employmentStatus || "active"]}</span></div><h2 className="mt-4 font-bold text-[#294535]">{employee.name || "موظف بلا اسم"}</h2><p className="mt-1 text-sm text-[#65796d]">{employee.profile?.jobTitle || "لم يُحدد المسمى الوظيفي"}</p><div className="mt-5 grid grid-cols-2 gap-3 border-t border-[#edf1ed] pt-4 text-xs"><div><p className="text-[#89958d]">القسم</p><p className="mt-1 font-bold text-[#3a5646]">{employee.department?.name || "غير محدد"}</p></div><div><p className="text-[#89958d]">رقم الموظف</p><p className="mt-1 font-bold text-[#3a5646]">{employee.profile?.employeeNumber || "—"}</p></div></div><Button variant="outline" onClick={() => openProfile(employee)} className="mt-5 h-9 w-full rounded-xl border-[#d5e3d7] text-[#386f50]"><UserRoundCheck className="ml-2 size-4" />إدارة الملف</Button></article>)}</div> : <StateCard title="لا توجد حسابات مفعّلة بعد" text="بعد تفعيل الحسابات من إدارة الوصول، ستظهر هنا لتجهيز ملفاتها الوظيفية." />}</div><Dialog open={departmentOpen} onOpenChange={setDepartmentOpen}><DialogContent dir="rtl"><DialogHeader><DialogTitle>قسم جديد</DialogTitle><DialogDescription>يُحفظ القسم داخل الشركة الحالية فقط ويمكن ربطه بملفات الموظفين.</DialogDescription></DialogHeader><div className="grid gap-4 py-3"><Field label="اسم القسم"><Input value={departmentName} onChange={event => setDepartmentName(event.target.value)} placeholder="مثال: العمليات" /></Field><Field label="رمز القسم"><Input value={departmentCode} onChange={event => setDepartmentCode(event.target.value)} placeholder="اختياري: OPS" /></Field></div><DialogFooter><Button variant="outline" onClick={() => setDepartmentOpen(false)}>إلغاء</Button><Button onClick={() => createDepartment.mutate({ name: departmentName.trim(), ...(departmentCode.trim() ? { code: departmentCode.trim() } : {}) })} disabled={createDepartment.isPending || departmentName.trim().length < 2} className="bg-[#1f5b45] hover:bg-[#174735]">حفظ القسم</Button></DialogFooter></DialogContent></Dialog><Dialog open={selectedId !== null} onOpenChange={open => !open && setSelectedId(null)}><DialogContent dir="rtl" className="max-h-[90vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>ملف {selected?.name || "الموظف"}</DialogTitle><DialogDescription>تُحفظ بيانات الملف داخل الشركة الحالية ولا تتيح صلاحيات جديدة بذاتها.</DialogDescription></DialogHeader><div className="grid gap-4 py-3"><Field label="رقم الموظف"><Input value={employeeNumber} onChange={event => setEmployeeNumber(event.target.value)} placeholder="مثال: EMP-104" /></Field><Field label="المسمى الوظيفي"><Input value={jobTitle} onChange={event => setJobTitle(event.target.value)} placeholder="مثال: منسق عمليات" /></Field><div className="grid gap-4 sm:grid-cols-2"><Field label="القسم"><Select value={departmentId} onValueChange={setDepartmentId}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">غير محدد</SelectItem>{departments?.map(department => <SelectItem key={department.id} value={String(department.id)}>{department.name}</SelectItem>)}</SelectContent></Select></Field><Field label="المدير المباشر"><Select value={managerId} onValueChange={setManagerId}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="none">غير محدد</SelectItem>{employees?.filter(employee => employee.id !== selectedId).map(employee => <SelectItem key={employee.id} value={String(employee.id)}>{employee.name || "موظف"}</SelectItem>)}</SelectContent></Select></Field></div><div className="grid gap-4 sm:grid-cols-2"><Field label="الحالة الوظيفية"><Select value={employmentStatus} onValueChange={value => setEmploymentStatus(value as EmploymentStatus)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{Object.entries(statusLabels).map(([value, label]) => <SelectItem key={value} value={value}>{label}</SelectItem>)}</SelectContent></Select></Field><Field label="تاريخ الانضمام"><Input type="date" value={joinedAt} onChange={event => setJoinedAt(event.target.value)} /></Field></div></div><DialogFooter><Button variant="outline" onClick={() => setSelectedId(null)}>إلغاء</Button><Button onClick={save} disabled={saveProfile.isPending} className="bg-[#1f5b45] hover:bg-[#174735]">حفظ الملف</Button></DialogFooter></DialogContent></Dialog></DashboardLayout>;
}

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div><label className="mb-2 block text-xs font-bold text-[#61756a]">{label}</label>{children}</div>; }
function StateCard({ title, text, error = false }: { title: string; text: string; error?: boolean }) { return <div className="mt-8 flex min-h-72 flex-col items-center justify-center rounded-3xl border border-[#dfe9e1] bg-white px-6 text-center"><span className={`flex size-14 items-center justify-center rounded-3xl ${error ? "bg-[#fdecea] text-[#a94e48]" : "bg-[#eaf3eb] text-[#357b53]"}`}>{error ? <ShieldAlert className="size-6" /> : <UsersRound className="size-6" />}</span><h2 className="mt-5 font-bold text-[#294535]">{title}</h2><p className="mt-2 max-w-md text-sm leading-6 text-[#748178]">{text}</p></div>; }
